# Copy this file to .env next to docker-compose.yml and fill in the API key.
#
#   cp .env.example .env
#   docker compose up -d
#
# .env is gitignored; this example is not, so keep secrets out of it.

# --- required ---------------------------------------------------------------

# Immich API key: Immich web UI -> Account Settings -> API Keys.
# Compose refuses to start when this is empty, which is better than booting with
# no key and having every Immich request come back 401.
IMMICH_API_KEY=

# Exact permitted Immich origin. This is an allowlist, not a user-editable
# server address; use HTTPS unless this is an explicitly approved tailnet URL.
IMMICH_ALLOWED_ORIGINS=https://192.0.2.10
# The origin must use this literal IP address (an IP SAN is needed for HTTPS).
# This eliminates DNS-rebinding risk rather than merely checking DNS once.
IMMICH_ALLOWED_IPS=192.0.2.10

# Generate once with a password manager / secrets tool. EPF refuses to expose
# the admin UI or device endpoints until all three values are set.
# Example password hash: python -c "from werkzeug.security import generate_password_hash; print(generate_password_hash('choose-a-long-password'))"
EPF_ADMIN_PASSWORD_HASH=
EPF_DEVICE_TOKEN=
EPF_SESSION_SECRET=

# --- optional, shown with their defaults ------------------------------------

# Host port for the settings page, and the port the frame connects to. It has to
# match the server URL saved in the frame's captive portal.
EPF_PORT=15001

# Leave this as loopback unless a reverse proxy on another machine must reach
# EPF. Bind to one host LAN address, never 0.0.0.0, and firewall the port so
# only the proxy can connect.
EPF_BIND_ADDRESS=127.0.0.1

# Local timezone. The base image has none, so leaving this unset makes
# datetime.now() return UTC: /sleep would then evaluate the quiet hours and the
# wake-up schedule in UTC and they would land at the wrong time of day.
TZ=Asia/Taipei

# --- low battery notifications ----------------------------------------------
#
# Nothing to set here. Telegram and LINE are linked from the settings page, which
# stores the credentials in /config/credentials.json so they can be changed
# without recreating the container. A service only counts as linked once a test
# message has actually been delivered.
