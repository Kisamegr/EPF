// Copy to device_secrets.h before compiling. Never commit the populated file.
// Generate a different token for every frame; it must match EPF_DEVICE_TOKEN on the server.
#define EPF_DEVICE_TOKEN "replace-with-a-long-random-device-token"
// PEM-encoded CA certificate for the HTTPS server. Leave this block commented
// only for a Tailscale-only HTTP deployment, where the tailnet authenticates
// the transport. This is a C++ string, not a multi-line #define.
// #define EPF_HAS_SERVER_CA_CERT 1
// static const char EpfServerCaCert[] = R"EOF(-----BEGIN CERTIFICATE-----
// ...
// -----END CERTIFICATE-----)EOF";
