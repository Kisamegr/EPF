#ifndef TAILSCALE_SECRETS_H
#define TAILSCALE_SECRETS_H

// Create Arduino/tailscale_secrets.h from this file and replace the value.
// Do not commit the real file. Use a reusable, pre-approved auth key with a
// tag restricted to this frame if your tailnet policy supports tags.
#define TAILSCALE_AUTH_KEY "tskey-auth-replace-me"
#define TAILSCALE_DEVICE_NAME "epf-frame"

#endif
